scenarios:
  highVolumeWrites:
    enabled: true
    description: "Write 1 TB per day"
    writers:
      replicas: 12
      args:
        source: synthetic
        loki-labels: host
        log-lines-rate: 1000
        synthetic-payload-size: 1000
