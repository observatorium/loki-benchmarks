scenarios:
  highVolumeWrites:
    enabled: false
    description: "Write 250 GB per day"
    samples:
      total: 2
      interval: "1m"
    writers:
      replicas: 3
      args:
        source: synthetic
        loki-labels: host
        log-lines-rate: 1000
        synthetic-payload-size: 1000