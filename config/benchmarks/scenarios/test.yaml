scenarios:
  highVolumeWrites:
    enabled: true
    samples:
      total: 2
      interval: "1m"
    configurations:
      - description: "Write 500 MB per day"
        writers:
          replicas: 1
          args:
            source: synthetic
            destination: loki
            log-lines-rate: 60
            synthetic-payload-size: 150
  highVolumeReads:
    enabled: true
    generator:
      replicas: 1
      args:
        source: application
        destination: loki
        log-lines-rate: 200
    startThreshold: 50000
    samples:
      total: 2
      interval: "1m"
    configurations:
      - description: "Query range 1 second"
        readers:
          replicas: 1
          queries:
            sumRateByLevel: 'sum by (level) (rate({client="promtail"} [1s]))'
          args:
            destination: loki
            log-lines-rate: 10
            query-range: "1s"
